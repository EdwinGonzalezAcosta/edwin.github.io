[build]
  command = "pip install -r requirements.txt && python manage.py collectstatic --noinput"
  publish = "templates"  # Ajusta esto al directorio donde se generan los archivos estáticos

[[redirects]]
  from = "/*"
  to = "/"
  status = 200
  force = true